function funChallenge(input) {
    let a = 10; // constant 0(1)
    a = 50 + 3; // constant 0(1)

    // O(n) linear 
    for (let i = 0; i < input.lenght; i++) {

        anotherFunction (); // O(n)
        let stranger = true  // O(n)
        a++ // O(n)
    }

    return a; // 0(1)
}

 //BIG 0(3 + 4n) becomes O(n)